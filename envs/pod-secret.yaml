apiVersion: v1
kind: Pod
metadata:
  name: secret-pod
spec:
  containers:
  - name: sec-app
    image: busybox:1.36
    command: ["sh", "-c", "sleep 3600"]
    envFrom:
      - secretRef:
          name: secret-name
      - secretRef:
          name: user-creds