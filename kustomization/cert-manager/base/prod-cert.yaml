apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lab-cert
  namespace: default
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  secretName: lab-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - lab.faberge-eggs.ru