apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgres.headlessName" . }}
spec:
  selector:
    {{- include "postgres.selectorLabels" . | nindent 4}}
  clusterIP: None
  ports:
    - port: {{ include "postgres.pg_port" . }}
      targetPort: postgres