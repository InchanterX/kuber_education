apiVersion: v1
kind: Service
metadata:
  name: {{ include "redpanda.fullname" . }}-service
spec:
  selector:
    {{- include "redpanda.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ include "redpanda.kafka_port" . }}
      targetPort: kafka-api